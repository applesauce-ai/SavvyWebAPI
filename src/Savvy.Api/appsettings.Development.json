{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "KeyVault": {
    "//": "Dev only: load secrets from the mock vault (keyvault.mock.json) instead of Azure.",
    "UseMock": true
  }
}
